This file documents the usage of `grep`.

- Search text in directory

``` shell
# -l, --files-with-matches
# -R, -r, --recursive
#     Recursively search subdirectories listed.
grep -lr TEXT_PATTERN .
```